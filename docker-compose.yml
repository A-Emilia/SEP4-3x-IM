version: "3.9"

services:
  sep4-react-app:
    build: ./sep4-react-app
    ports:
      - "5173:5173"
    volumes:
      - ./sep4-react-app:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true
    tty: true